# Test-only dependencies (not needed for production runtime).
# Install with:  pip install -r requirements-dev.txt
-r requirements.lock
pytest==9.1.0
# Starlette 1.2 uses httpx2 for TestClient; installing it removes the legacy
# httpx compatibility warning while Supabase can retain its httpx dependency.
httpx2==2.10.0
